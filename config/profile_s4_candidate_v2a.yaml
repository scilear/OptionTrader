profile_id: candidate_multi_signal_v2a

overrides:
  alerts:
    policy_variant: v2a
  regime:
    weights:
      vix: 0.25
      rv20: 0.25
      drawdown: 0.25
      event: 0.15
      stress_proxy: 0.10
